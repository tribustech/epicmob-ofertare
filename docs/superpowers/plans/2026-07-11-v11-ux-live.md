# v1.1 — Ansambluri, prețuri live, shadcn/ui (Plan 4) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Addendum v1.1 din spec: ansambluri cu picioare uniforme, editor de corp condiționat cu preț live în browser, prețuri live pentru ciorne + îngheț automat la Trimisă, și shadcn/ui pe toată aplicația cu layout orizontal.

**Architecture:** Motorul (`lib/engine`) rămâne neatins și rulează și client-side în editor (e pur). Nivelul nou `Assembly` stă între Project și Cabinet (Cabinet.assemblyId opțional pentru compatibilitate; corpurile vechi se migrează cu un script). Baza de calcul devine `getQuoteBasis`: CIORNĂ → snapshot construit pe loc din cataloagele curente (live); TRIMISĂ/ACCEPTATĂ → `snapshotJson` (înghețat automat la tranziție). Estimarea per corp e o funcție pură nouă (`estimateCabinetCost`) care evită rotunjirea la foi. Task-urile de re-stilizare (1, 2, 6, 7) sunt refactoruri cu cerințe precise + gate build/test, nu transcriere: implementatorul are libertate pe detaliile vizuale în limitele cerințelor.

**Tech Stack:** existent + shadcn/ui (CLI, stil "new-york", bază neutral) cu dependențele lui (radix-ui, class-variance-authority, clsx, tailwind-merge, lucide-react).

## Global Constraints

- Motorul `lib/engine/**` NU se modifică. Logica nouă de calcul (leg pick, estimare) intră în `lib/quote/` ca funcții pure testate.
- Comportament prețuri: CIORNĂ = live (fără buton de calcul); tranziția CIORNĂ→TRIMISĂ (sau →ACCEPTATĂ) face snapshot automat; TRIMISĂ/ACCEPTATĂ citesc DOAR din snapshot la rezumat/ofertă/rute CSV, cu badge „Prețuri înghețate la {data}" și acțiune „Reîmprospătează prețurile înghețate"; revenire la CIORNĂ → live din nou (snapshot-ul stocat rămâne, ignorat).
- Picior per ansamblu: la rezolvarea sugestiilor, pentru categoria PICIOR se alege itemul activ cu `nominalLengthMm === legHeightMm` al ansamblului (cel mai ieftin la egalitate); fallback `defaults.legId`.
- Editorul de corp: tip = radio; câmpurile de sertare NU se randează pentru tipuri cu uși și invers (polițe/uși ascunse la SERTARE); estimarea live rulează client-side prin `cabinetFormSchema` + `expandCabinet` + `estimateCabinetCost` cu try/catch (eroarea se afișează, nu crapă).
- shadcn/ui: componentele proprii (`TextInput`, `NumberInput`, `Select`, `SubmitButton`, `ActionForm`, `DeleteButton`) devin wrappere peste primitivele shadcn păstrându-și numele și props-urile (paginile existente nu-și schimbă contractul); layout container `max-w-screen-2xl`; formularele de listă pe orizontală (grid-uri), nu stivuite.
- Texte RO. `npm test` și `npm run build` trec după fiecare task; commit per task.

## File Structure (nou/modificat major)

```
├── components/ui/*            # shadcn (generat de CLI)
├── lib/utils.ts               # cn() (shadcn)
├── components/forms.tsx       # wrappere peste shadcn (același API)
├── components/ActionForm.tsx  # eroare prin <Alert variant="destructive">
├── components/DeleteButton.tsx
├── components/CabinetEditorForm.tsx  # NOU: client, formular + piese + preț live
├── lib/quote/
│   ├── legs.ts                # pickLegId (pur) (Task 4)
│   ├── estimate.ts            # estimateCabinetCost (pur) (Task 4)
│   ├── basis.ts               # getQuoteBasis + shouldFreeze (Task 5)
│   ├── compute.ts             # + legHeightMm per QuoteCabinet (Task 4)
│   ├── actions.ts             # + assembly actions, freeze logic, updateCabinet cu obiect (Task 3/5/8)
│   └── load.ts                # + assemblies (Task 3)
├── scripts/backfill-assemblies.ts    (Task 3)
└── prisma/schema.prisma       # + Assembly, Cabinet.assemblyId (Task 3)
```

---

### Task 1: shadcn/ui — instalare + layout

**Files:** `components.json`, `lib/utils.ts`, `components/ui/*`, `app/globals.css`, `app/layout.tsx`, `package.json`

- [ ] **Step 1:** Instalează shadcn: `npx shadcn@latest init -y` (stil implicit, culoare de bază neutral, CSS variables). Dacă CLI-ul cere opțiuni, alege: TypeScript, stil "new-york", bază "neutral", CSS variables da, alias `@/components`/`@/lib/utils` (există deja în tsconfig).
- [ ] **Step 2:** Adaugă componentele: `npx shadcn@latest add button input select label card table alert badge separator radio-group checkbox`.
- [ ] **Step 3:** Layout: în `app/layout.tsx` lărgește containerele (`max-w-6xl` → `max-w-screen-2xl`) la nav și main; navul folosește componenta `Button` variant="ghost" size="sm" pentru linkuri sau păstrează Link-uri stilizate — cerință: navul rămâne funcțional cu aceleași rute + `print:hidden`.
- [ ] **Step 4:** Verifică: `npm run build && npm test` (102/102; globals.css cu temă shadcn nu strică nimic). Dacă CLI-ul shadcn eșuează din motive de rețea/prompturi, oprește-te și raportează BLOCKED (nu scrie componente de mână fără aprobat).
- [ ] **Step 5:** Commit: `feat: shadcn/ui foundation and wide layout`

### Task 2: Wrappere de formular + restilizare cataloage

**Files:** `components/forms.tsx`, `components/ActionForm.tsx`, `components/DeleteButton.tsx`, cele 6 pagini de catalog.

Cerințe (refactor, nu transcriere):
- `TextInput`/`NumberInput`: `<Label>` + shadcn `<Input type=...>`, aceleași props (name, label, defaultValue, required, step, min).
- `Select` (al nostru): `<Label>` + `<select>` nativ stilizat cu clasele shadcn de input (NU Radix Select — formularul e server-side, are nevoie de `<select name=...>` nativ) — păstrează props (name, label, options, defaultValue, allowEmpty).
- `SubmitButton` → shadcn `<Button type="submit">`. `DeleteButton` → `<Button variant="destructive" size="sm">` + confirm() ca acum. `ActionForm` afișează eroarea prin `<Alert variant="destructive">`.
- Cele 6 pagini de catalog: fiecare intrare de listă devine `<Card>` (sau rând de `<Table>` unde e mai potrivit — la manoperă și debitare tabelul e mai bun); formularele rămân pe grid-uri orizontale; titlurile paginilor consistente. Nicio schimbare de contract cu actions.
- [ ] **Step 1:** Rescrie componentele comune conform cerințelor. **Step 2:** Restilizează cele 6 pagini. **Step 3:** `npm run build && npm test` (102/102) + smoke vizual: `(npm run start &) && sleep 3 && curl -s localhost:3000/cataloage/materiale | grep -o "Materiale plăci" && pkill -f "next start"`. **Step 4:** Commit: `feat: shadcn form primitives and restyled catalog pages`

### Task 3: Modelul Assembly + migrare + backfill + actions

**Files:** `prisma/schema.prisma`, `scripts/backfill-assemblies.ts`, `lib/quote/actions.ts`, `lib/quote/load.ts`

- [ ] **Step 1:** Schema — adaugă modelul și relația:
```prisma
model Assembly {
  id          String    @id @default(cuid())
  projectId   String
  project     Project   @relation(fields: [projectId], references: [id], onDelete: Cascade)
  name        String
  legHeightMm Float     @default(100)
  sortOrder   Int       @default(0)
  cabinets    Cabinet[]
}
```
În `Project` adaugă `assemblies Assembly[]`. În `Cabinet` adaugă:
```prisma
  assemblyId String?
  assembly   Assembly? @relation(fields: [assemblyId], references: [id], onDelete: SetNull)
```
Run: `npx prisma migrate dev --name assemblies`.

- [ ] **Step 2:** `scripts/backfill-assemblies.ts` (tsx): pentru fiecare proiect cu corpuri fără `assemblyId`, creează un Assembly `„Ansamblu 1"` (legHeightMm 100) și asignează corpurile. Idempotent (sare peste corpurile deja asignate). Rulează-l: `npx tsx scripts/backfill-assemblies.ts`.

- [ ] **Step 3:** Actions noi în `lib/quote/actions.ts` (toate prin `formAction`):
```ts
const assemblySchema = z.object({
  name: z.string().trim().min(1, 'Numele ansamblului lipsește'),
  legHeightMm: z.coerce.number().positive(),
});

export const addAssembly = formAction(async (projectId: string, fd: FormData) => {
  const d = assemblySchema.parse(formDataToObject(fd));
  const count = await prisma.assembly.count({ where: { projectId } });
  await prisma.assembly.create({ data: { projectId, name: d.name, legHeightMm: d.legHeightMm, sortOrder: count } });
  revalidatePath(`/proiecte/${projectId}`);
});

export const updateAssembly = formAction(async (assemblyId: string, fd: FormData) => {
  const d = assemblySchema.parse(formDataToObject(fd));
  const a = await prisma.assembly.update({ where: { id: assemblyId }, data: { name: d.name, legHeightMm: d.legHeightMm } });
  revalidatePath(`/proiecte/${a.projectId}`);
});

export const deleteAssembly = formAction(async (assemblyId: string) => {
  const count = await prisma.cabinet.count({ where: { assemblyId } });
  if (count > 0) throw new Error('Ansamblul are corpuri — mută-le sau șterge-le întâi.');
  const a = await prisma.assembly.delete({ where: { id: assemblyId } });
  revalidatePath(`/proiecte/${a.projectId}`);
});
```
`addCabinet` primește `assemblyId` (signature `(projectId: string, assemblyId: string)`) și îl scrie pe corp; validează că ansamblul aparține proiectului (throw altfel).

- [ ] **Step 4:** `lib/quote/load.ts`: `loadProject` include `assemblies` (ordonate) și fiecare `LoadedCabinet` capătă `assemblyId: string | null`; adaugă `legHeightByCabinet` derivabil (map assemblyId→legHeightMm). Nu strica semnăturile existente — extinde tipurile.
- [ ] **Step 5:** `npm run build && npm test` (102/102). Commit: `feat: assembly model with backfill and actions`

### Task 4: Leg pick per ansamblu + estimare live per corp (pur, TDD)

**Files:** `lib/quote/legs.ts`, `lib/quote/estimate.ts`, `lib/quote/compute.ts` (extins), teste noi.

- [ ] **Step 1: Teste care pică** — `lib/quote/__tests__/legs.test.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { pickLegId } from '../legs';

const HW = [
  { id: 'p100-scump', name: 'Picior 100 premium', category: 'PICIOR', pricePerUnit: 5, nominalLengthMm: 100, loadClassKg: null, active: true },
  { id: 'p100', name: 'Picior 100', category: 'PICIOR', pricePerUnit: 2.5, nominalLengthMm: 100, loadClassKg: null, active: true },
  { id: 'p150', name: 'Picior 150', category: 'PICIOR', pricePerUnit: 3, nominalLengthMm: 150, loadClassKg: null, active: true },
  { id: 'p100-inactiv', name: 'Picior 100 vechi', category: 'PICIOR', pricePerUnit: 1, nominalLengthMm: 100, loadClassKg: null, active: false },
];

describe('pickLegId', () => {
  it('alege piciorul activ cu nominala potrivită, cel mai ieftin', () => {
    expect(pickLegId(HW, 100, 'fallback')).toBe('p100');
    expect(pickLegId(HW, 150, 'fallback')).toBe('p150');
  });
  it('fără potrivire → fallback', () => {
    expect(pickLegId(HW, 120, 'fallback')).toBe('fallback');
    expect(pickLegId([], 100, null)).toBeNull();
  });
});
```
și `lib/quote/__tests__/estimate.test.ts` (folosește fixtures existente `makeSnapshot`, `refCabinet`):
```ts
import { describe, expect, it } from 'vitest';
import { estimateCabinetCost } from '../estimate';
import { makeSnapshot, refCabinet } from './fixtures';

describe('estimateCabinetCost', () => {
  it('corpul de referință: estimare fără rotunjire la foi, cu manoperă și feronerie auto', () => {
    const r = estimateCabinetCost(
      { input: refCabinet(), hardwareOverrides: null, extraParts: [] },
      makeSnapshot(), { markupPct: 30, yieldFactor: 0.8, legHeightMm: null },
    );
    // plăci fracționar: PAL 1.737/(5.796×0.8)=0.3746 foi ×260=97.40 + debitare 0.3746×50=18.73
    // PFL 0.4267/(5.8995×0.8)=0.0904 foi ×100=9.04 + 0.0904×33=2.98
    // MDF 0.4267×450=192.03; cant 3.13; feronerie 48; manoperă 150
    expect(r.cost).toBeCloseTo(521.32, 0);
    expect(r.sell).toBeCloseTo(677.72, 0);
    expect(r.error).toBeNull();
  });
  it('dimensiuni imposibile → error, nu throw', () => {
    const bad = refCabinet(); bad.widthMm = 10;
    const r = estimateCabinetCost(
      { input: bad, hardwareOverrides: null, extraParts: [] },
      makeSnapshot(), { markupPct: 30, yieldFactor: 0.8, legHeightMm: null },
    );
    expect(r.error).toBeTruthy();
    expect(r.cost).toBe(0);
  });
});
```
Run ambele → FAIL (module lipsă).

- [ ] **Step 2: Implementează**

`lib/quote/legs.ts`:
```ts
import type { HardwareRow } from '@/lib/catalog/convert';

// alege piciorul activ cu nominala == legHeightMm (cel mai ieftin); fallback la id-ul implicit
export function pickLegId(
  hardware: (HardwareRow & { active: boolean })[],
  legHeightMm: number,
  fallback: string | null,
): string | null {
  const candidates = hardware
    .filter((h) => h.active && h.category === 'PICIOR' && h.nominalLengthMm === legHeightMm)
    .sort((a, b) => a.pricePerUnit - b.pricePerUnit);
  return candidates[0]?.id ?? fallback;
}
```

`lib/quote/estimate.ts`:
```ts
import {
  buildHardwareDefaults, parseConstruction, toCostCatalogs,
} from '@/lib/catalog/convert';
import { expandCabinet, resolveSuggestions } from '@/lib/engine';
import type { HardwareLine } from '@/lib/engine';
import { pickLegId } from './legs';
import type { QuoteCabinet, SnapshotData } from './compute';

export interface CabinetEstimate {
  cost: number;
  sell: number;
  error: string | null;
}

// Estimare per corp pentru afișare live: plăci PER_SHEET pe arie (foi fracționare, cu factor
// de utilizare) + debitare proporțională; NU e prețul final (acela rotunjește foile pe proiect).
export function estimateCabinetCost(
  cabinet: QuoteCabinet,
  snap: SnapshotData,
  opts: { markupPct: number; yieldFactor: number; legHeightMm: number | null },
): CabinetEstimate {
  try {
    const catalogs = toCostCatalogs(snap.materials, snap.edgeBands, snap.hardware, snap.cuttingRates, snap.laborRates);
    const defaults = buildHardwareDefaults(snap.hardware.filter((h) => h.active), snap.settings);
    if (opts.legHeightMm !== null) {
      defaults.legId = pickLegId(snap.hardware, opts.legHeightMm, defaults.legId);
    }
    const cc = parseConstruction(snap.settings.constructionJson);
    const expanded = expandCabinet(cabinet.input, catalogs, cc);

    const parts = [...expanded.parts];
    for (const p of cabinet.extraParts) {
      parts.push({
        cabinetLabel: cabinet.input.label, name: p.name,
        lengthMm: p.lengthMm, widthMm: p.widthMm, qty: p.qty,
        materialId: p.materialId, edges: {},
      });
    }

    let boards = 0;
    let cutting = 0;
    let edging = 0;
    const areaByMaterial = new Map<string, number>();
    for (const p of parts) {
      const area = (p.lengthMm / 1000) * (p.widthMm / 1000) * p.qty;
      areaByMaterial.set(p.materialId, (areaByMaterial.get(p.materialId) ?? 0) + area);
      for (const [edge, mm] of [
        [p.edges.l1, p.lengthMm], [p.edges.l2, p.lengthMm],
        [p.edges.w1, p.widthMm], [p.edges.w2, p.widthMm],
      ] as const) {
        if (!edge) continue;
        const band = catalogs.edgeBands.find((b) => b.id === edge);
        if (!band) throw new Error(`Cant inexistent în catalog: ${edge}`);
        edging += (mm / 1000) * p.qty * band.pricePerMl;
      }
    }
    const rates = [...catalogs.cuttingRates].sort((a, b) => a.maxThicknessMm - b.maxThicknessMm);
    for (const [materialId, area] of areaByMaterial) {
      const m = catalogs.materials.find((x) => x.id === materialId);
      if (!m) throw new Error(`Material inexistent în catalog: ${materialId}`);
      if (m.pricing.mode === 'PER_SQM') {
        boards += area * m.pricing.pricePerSqm;
      } else {
        const sheetArea = (m.sheetLengthMm / 1000) * (m.sheetWidthMm / 1000);
        const fractionalSheets = area / (sheetArea * opts.yieldFactor);
        boards += fractionalSheets * m.pricing.pricePerSheet;
        const rate = rates.find((r) => r.maxThicknessMm >= m.thicknessMm);
        if (rate) cutting += fractionalSheets * rate.pricePerSheet;
      }
    }

    const lines: HardwareLine[] = cabinet.hardwareOverrides ?? resolveSuggestions(expanded.hardware, defaults).lines;
    let hardware = 0;
    for (const line of lines) {
      const item = catalogs.hardware.find((h) => h.id === line.hardwareId);
      if (!item) throw new Error(`Feronerie inexistentă în catalog: ${line.hardwareId}`);
      hardware += line.qty * item.pricePerUnit;
    }

    const labor = catalogs.laborPerType[cabinet.input.type];
    const cost = boards + cutting + edging + hardware + labor;
    return { cost, sell: cost * (1 + opts.markupPct / 100), error: null };
  } catch (e) {
    return { cost: 0, sell: 0, error: e instanceof Error ? e.message : 'Eroare de calcul' };
  }
}
```

`lib/quote/compute.ts`: `QuoteCabinet` capătă câmp opțional `legHeightMm?: number | null`; în bucla de rezolvare, când corpul are `legHeightMm` non-null, folosește un obiect defaults clonat cu `legId: pickLegId(snap.hardware, legHeightMm, defaults.legId)`. Adaugă test în `compute.test.ts`:
```ts
  it('picioarele urmează înălțimea ansamblului', () => {
    const snap = makeSnapshot();
    snap.hardware.push({ id: 'p150', name: 'Picior 150', category: 'PICIOR', pricePerUnit: 3, nominalLengthMm: 150, loadClassKg: null, active: true });
    const q = baseQuote();
    q.cabinets[0].legHeightMm = 150;
    const r = computeQuote(q, snap);
    expect(r.hardwareLines).toContainEqual({ hardwareId: 'p150', qty: 4 });
  });
```
(fixture-ul `makeSnapshot` are `picior-std` fără nominală → fără potrivire la 150 fără itemul nou.)

- [ ] **Step 3:** `npm test` — toate verzi (102 + 2 legs + 2 estimate + 1 compute = 107). `npm run build`. Commit: `feat: per-assembly leg pick and live cabinet estimate`

### Task 5: Baza de calcul live/înghețat + îngheț automat la Trimisă

**Files:** `lib/quote/basis.ts` (nou), `lib/quote/actions.ts` (updateProjectSettings + refreshFrozenPrices; recalculateProject se elimină), `lib/quote/load.ts`, `app/proiecte/[id]/page.tsx` + rutele CSV + oferta (folosesc basis).

- [ ] **Step 1:** `lib/quote/basis.ts`:
```ts
import type { Project } from '@prisma/client';
import { buildSnapshot } from './snapshot';
import type { SnapshotData } from './compute';

export type QuoteBasis =
  | { kind: 'LIVE'; snapshot: SnapshotData }
  | { kind: 'FROZEN'; snapshot: SnapshotData }
  | { kind: 'MISSING' };

export function isFrozenStatus(status: string): boolean {
  return status === 'TRIMISA' || status === 'ACCEPTATA';
}

export async function getQuoteBasis(project: Pick<Project, 'status' | 'snapshotJson'>): Promise<QuoteBasis> {
  if (isFrozenStatus(project.status)) {
    if (!project.snapshotJson) return { kind: 'MISSING' };
    return { kind: 'FROZEN', snapshot: JSON.parse(project.snapshotJson) as SnapshotData };
  }
  return { kind: 'LIVE', snapshot: await buildSnapshot() };
}
```
- [ ] **Step 2:** În `updateProjectSettings`: după parse, citește proiectul curent; dacă `isFrozenStatus(d.status)` și (`!isFrozenStatus(project.status)` sau `!project.snapshotJson`) → `data.snapshotJson = JSON.stringify(await buildSnapshot())`. Înlocuiește `recalculateProject` cu:
```ts
export const refreshFrozenPrices = formAction(async (projectId: string) => {
  const project = await prisma.project.findUniqueOrThrow({ where: { id: projectId } });
  if (!isFrozenStatus(project.status)) throw new Error('Proiectul e ciornă — prețurile sunt deja live.');
  await prisma.project.update({
    where: { id: projectId },
    data: { snapshotJson: JSON.stringify(await buildSnapshot()) },
  });
  revalidatePath(`/proiecte/${projectId}`);
});
```
(șterge exportul `recalculateProject`; actualizează importurile din pagini).
- [ ] **Step 3:** Pagina de proiect, oferta și cele două rute CSV trec pe `getQuoteBasis`: LIVE → calculează și afișează normal cu badge „Prețuri live"; FROZEN → badge „Prețuri înghețate la {takenAt}" + buton `refreshFrozenPrices`; MISSING (teoretic imposibil) → mesaj + îndemn să comuți starea. Rutele CSV: LIVE se acceptă (ciorna exportă cu prețuri curente); scoate răspunsurile 400 „fără snapshot".
- [ ] **Step 4:** `npm run build && npm test` (107/107 — nimic pur nou de testat aici; buildSnapshot deja testat indirect). Actualizează `scripts/smoke-quote.ts` dacă folosea recalculateProject (nu — folosește buildSnapshot direct; doar verifică că mai compilează). Commit: `feat: live pricing for drafts with auto-freeze on sent status`

### Task 6: Pagina de proiect — ansambluri + totaluri live + restilizare

**Files:** `app/proiecte/[id]/page.tsx`, `app/proiecte/page.tsx` (restilizare listă).

Cerințe:
- Secțiune „Ansambluri": card per ansamblu cu nume + legHeight (form `updateAssembly` inline), listă corpuri ale ansamblului (tabel: etichetă/link, tip, dimensiuni, „feronerie editată" badge, Duplică, Șterge), buton „Adaugă corp" per ansamblu (`addCabinet.bind(null, project.id, a.id)`), `DeleteButton` pe ansamblu (mesajul de eroare apare dacă are corpuri). Form „Ansamblu nou": select preselecții (Bucătărie, Dressing, Corp baie, Living, Hol, Altul) + input nume liber (dacă „Altul") — simplu: un `<select>` de preselecții care completează un text input via componentă client mică SAU două câmpuri (select `namePreset` + text opțional `name`) rezolvate server-side (name = text liber dacă dat, altfel preset). Alege varianta fără JS: două câmpuri + regulă server-side documentată în UI.
- Corpuri rămase fără ansamblu (assemblyId null): secțiune „Fără ansamblu" doar dacă există.
- Totalurile din `computeQuote` primesc `legHeightMm` per corp din ansamblu (via `toQuoteInput` extins — adaugă parametrul).
- Rezumatul: mereu vizibil (LIVE sau FROZEN după basis), fără buton „Calculează"; categorii + total + vânzare + lei/ml pe orizontală (grid de `Card`-uri pentru cifrele mari, tabel pentru categorii); exporturi ca linkuri `Button` variant="outline".
- Layout general pe 2 coloane la desktop (stânga: ansambluri+corpuri+linii libere; dreapta: setări + rezumat + exporturi), `lg:grid-cols-[2fr,1fr]`.
- [ ] Step 1 implementare; Step 2 `npm run build && npm test`; Step 3 commit `feat: project page with assemblies and live totals`

### Task 7: Oferta + lista proiecte — restilizare și basis

**Files:** `app/proiecte/[id]/oferta/page.tsx`, rutele CSV (basis din Task 5 deja), `app/page.tsx` (link-uri card).
Cerințe: oferta grupată pe ansambluri (heading per ansamblu, corpurile lui în tabel), badge intern (doar pe ecran, `print:hidden`) LIVE/FROZEN, restul identic; lista proiecte cu `Card`-uri pe grid orizontal (2-3 coloane desktop) în loc de listă verticală.
- [ ] Step 1 implementare; Step 2 build+test; Step 3 commit `feat: assembly-grouped offer and restyled project list`

### Task 8: Editorul de corp client-side cu preț live

**Files:** `components/CabinetEditorForm.tsx` (nou, client), `app/proiecte/[id]/corp/[cabinetId]/page.tsx` (rescris), `lib/quote/actions.ts` (+`updateCabinetData`).

- [ ] **Step 1:** Action nou care primește obiect (nu FormData):
```ts
export const updateCabinetData = formAction(async (cabinetId: string, data: Record<string, string>) => {
  const d = cabinetFormSchema.parse(data);
  const input = toCabinetInput(d);
  const cab = await prisma.cabinet.update({ where: { id: cabinetId }, data: { inputJson: JSON.stringify(input) } });
  revalidatePath(`/proiecte/${cab.projectId}/corp/${cabinetId}`);
  revalidatePath(`/proiecte/${cab.projectId}`);
});
```
- [ ] **Step 2:** `components/CabinetEditorForm.tsx` — client component. Props (toate serializabile): `cabinetId`, `initial: Record<string,string>` (valorile formularului derivate din CabinetInput salvat), `snapshot: SnapshotData` (live, construit în pagină), `markupPct`, `yieldFactor`, `legHeightMm: number | null`, `materialOptions/bandOptions` (cu „(dezactivat)" deja aplicat), `hardwareOverrides: HardwareLine[] | null`, `extraParts: ExtraPart[]`, `save: (data: Record<string,string>) => Promise<FormState>`.
Comportament:
  - Stare React pentru toate câmpurile (inițial din `initial`).
  - Tip corp = shadcn `RadioGroup` orizontal cu cele 5 tipuri.
  - Câmpuri condiționate: `SERTARE` → secțiunea sertare (nr., sistem, fund, înălțimi) și FĂRĂ uși/polițe; celelalte tipuri → uși + polițe și FĂRĂ sertare; `COLT` → + panou orb; checkbox spate → câmpurile spate.
  - La fiecare schimbare: `cabinetFormSchema.safeParse(stateAsStrings)` → dacă ok, `toCabinetInput` → `estimateCabinetCost({input, hardwareOverrides, extraParts}, snapshot, {markupPct, yieldFactor, legHeightMm})` și `expandCabinet` (în interiorul estimate-ului e deja; pentru piese refolosește `expandCabinet` separat într-un try/catch sau expune-le — SIMPLU: calculează piesele cu `expandCabinet` în try/catch local și estimarea cu `estimateCabinetCost`; ambele importate direct — sunt pure).
  - Layout: `grid lg:grid-cols-2 gap-6`: stânga formularul (grupat pe `Card`-uri: Identificare/Dimensiuni, Materiale și canturi, secțiunea condiționată, Spate); dreapta: `Card` Preț estimativ (cost + vânzare, mare; disclaimerul „estimativ — prețul final rotunjește foile pe proiect"), avertismente (Alert amber), tabel piese live.
  - Buton „Salvează corpul" → `useTransition` + `save(state)`; afișează eroarea din FormState; după succes `router.refresh()`.
  - Validation errors din safeParse: afișate compact sub buton (lista mesajelor), prețul rămâne pe ultima valoare validă cu badge „valori invalide".
- [ ] **Step 3:** Pagina editorului: server component care încarcă corpul + ansamblul (legHeightMm), construiește snapshot-ul live (`buildSnapshot()`), opțiunile cu `optionsWithCurrent` (păstrează logica existentă incl. avertismentul de referințe dezactivate) și randează `<CabinetEditorForm .../>` sus, apoi secțiunile existente Feronerie (cu hint-ul stale) și Piese suplimentare restilizate cu shadcn — logica lor rămâne identică (server forms). Preview-ul server-side vechi de piese dispare (l-a înlocuit cel live din client).
- [ ] **Step 4:** `npm run build && npm test` (107/107). Smoke: pornește `next start`, curl editorul unui corp existent din seed... (corpurile există doar dacă utilizatorul a creat — fă smoke doar pe `/proiecte`). Commit: `feat: client-side cabinet editor with live price and conditional fields`

### Task 9: Smoke final v1.1 + curățenie

**Files:** `scripts/smoke-quote.ts` (extins ușor).
- [ ] **Step 1:** Extinde smoke-ul: creează proiect + ansamblu (legHeightMm 150) + corp în ansamblu; verifică prin `computeQuote` cu `legHeightMm: 150` că nu aruncă și totalul > 0; șterge tot în finally. Rulează.
- [ ] **Step 2:** Verifică tot: `npm test` (107/107), `npm run build`, `npx tsx scripts/smoke-quote.ts`, smoke pagini (`/proiecte`, `/cataloage/materiale`).
- [ ] **Step 3:** Commit: `test: extend smoke for assemblies and live pricing`

## După acest plan

v1.1 complet. Validarea utilizatorului rămâne aceeași: prețuri reale + bucătărie reală. Backlog-ul v1.x rămâne în ledger (CSV escaping, soclu automat, mașină de spălat vase/frigider ca tipuri speciale, editare per-piesă).
